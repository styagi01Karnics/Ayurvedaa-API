# Ayurvedaa Auth — Frontend Integration Guide

This file is a **pointer** only.

**Primary documentation (complete):** [`AUTH_API_GUIDE.md`](./AUTH_API_GUIDE.md)

That guide covers:

- Base URL (`http://localhost:8111`), JWT header, `ApiResponse` envelope
- Procedures: Super Admin bootstrap, Super Admin login, hospital onboard/signup, hospital login, forgot/reset/change password
- Full API reference (auth, platform, users, roles) from live controllers/DTOs
- `tenants` vs `auth_users`, `tenantCode` BRAND-STATE generation

Use **only** `AUTH_API_GUIDE.md` going forward for login/signup/onboarding integration.
