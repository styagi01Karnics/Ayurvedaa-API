spring:
  application:
    name: therapist-service
  datasource:
    url: jdbc:postgresql://103.174.103.250:5432/ayurveda_db?currentSchema=public
    username: ayurveda
    password: "S@b8at1zeauyrveda"
    driver-class-name: org.postgresql.Driver
    hikari:
      connection-init-sql: SET search_path TO public
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        default_schema: public

server:
  port: 8104

services:
  # Overridden in Docker deploy via SERVICES_APPOINTMENT_URL (container DNS).
  # Do not use localhost inside Docker — that is the therapist container itself.
  appointment:
    url: http://103.174.103.250:8103

springdoc:
  api-docs:
    path: /api-docs
    enabled: true
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
    tags-sorter: alpha
    operations-sorter: alpha
    display-request-duration: true
  show-actuator: false

logging:
  level:
    org.springframework.web: INFO
    org.hibernate.SQL: DEBUG
