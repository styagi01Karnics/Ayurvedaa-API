spring:
  application:
    name: payment-service
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
    driver-class-name: org.postgresql.Driver
    hikari:
      pool-name: payment-service
      maximum-pool-size: 5
      minimum-idle: 1
      idle-timeout: 60000
      max-lifetime: 1800000
      keepalive-time: 30000
      connection-timeout: 10000
      leak-detection-threshold: 30000
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
      properties:
        max.block.ms: 3000
        request.timeout.ms: 3000
        retries: 1
        acks: 1
    admin:
      fail-fast: false
    listener:
      missing-topics-fatal: false

server:
  port: 8112

services:
  billing:
    url: ${BILLING_SERVICE_URL:http://127.0.0.1:8109}
  notification:
    # Email SMTP is in notification-service only; payment publishes via HTTP.
    url: ${NOTIFICATION_SERVICE_URL:http://127.0.0.1:8110}
    enabled: ${NOTIFICATION_SERVICE_ENABLED:true}

ayurveda:
  kafka:
    enabled: ${KAFKA_ENABLED:true}
  payment:
    pay-page-base-url: ${PAY_PAGE_BASE_URL:http://45.195.229.15:8112}
  tenant:
    routing:
      enabled: true
      require-hospital-schema: true
      skip-path-patterns:
        - /api/v1/payments/payu/**
        - /api/v1/payments/public/**
        - /pay/**

auth:
  jwt:
    secret: ${JWT_SECRET:AyurvedaaAuthJwtSecretKeyMustBeAtLeast256BitsLong!!}

# Single-tenant PayU (one hospital for now). Set on the server — do NOT commit live values.
# Super Admin per-tenant payment-gateway API is not used by payment-service.
#
# Example LIVE on server:
#   PAYU_MERCHANT_KEY=...
#   PAYU_MERCHANT_SALT=...
#   PAYU_MODE=LIVE
#   PAYU_PAYMENT_URL=https://secure.payu.in/_payment
# Optional OAuth (not required for hosted checkout hash):
#   PAYU_CLIENT_ID=...
#   PAYU_CLIENT_SECRET=...
payu:
  merchant-key: ${PAYU_MERCHANT_KEY:}
  merchant-salt: ${PAYU_MERCHANT_SALT:}
  client-id: ${PAYU_CLIENT_ID:}
  client-secret: ${PAYU_CLIENT_SECRET:}
  mode: ${PAYU_MODE:TEST}
  payment-url: ${PAYU_PAYMENT_URL:https://test.payu.in/_payment}
  postservice-url: ${PAYU_POSTSERVICE_URL:}
  success-url: ${PAYU_SUCCESS_URL:http://45.195.229.15:8112/api/v1/payments/payu/success}
  failure-url: ${PAYU_FAILURE_URL:http://45.195.229.15:8112/api/v1/payments/payu/failure}

springdoc:
  api-docs:
    path: /api-docs
    enabled: true
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
    tags-sorter: alpha
    operations-sorter: alpha
    display-request-duration: true
  show-actuator: false

logging:
  level:
    org.springframework.web: INFO
    org.hibernate.SQL: DEBUG
    com.ayurveda.payment: INFO

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      show-details: always
  metrics:
    tags:
      application: payment-service
  prometheus:
    metrics:
      export:
        enabled: true
